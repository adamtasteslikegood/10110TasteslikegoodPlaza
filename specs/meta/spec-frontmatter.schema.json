{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/adamtasteslikegood/10110TasteslikegoodPlaza/specs/meta/spec-frontmatter.schema.json",
  "title": "Plaza governed-document frontmatter",
  "description": "The technical contract every governed document in the 10110 TastesLike Plaza doc set carries in its YAML frontmatter. Authority is declared here, in machine-readable form, so 'which document wins' is a lookup rather than an argument. Governed by specs/meta/META-SPEC.md; enforced by scripts/validate_specs.py.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "doc_id",
    "title",
    "tier",
    "authority",
    "status",
    "doc_set_version",
    "last_updated"
  ],
  "properties": {
    "doc_id": {
      "type": "string",
      "description": "Stable identifier. Never changes when the file moves; cross-references cite this, not the path.",
      "pattern": "^[A-Z][A-Z0-9-]{2,39}$"
    },
    "title": {
      "type": "string",
      "description": "Human-readable name. Should match the document's H1.",
      "minLength": 3
    },
    "tier": {
      "type": "integer",
      "description": "Position in the hierarchy. Lower wins. 0 = meta layer (governs the doc set itself), 1 = concept source of truth, 2 = implementation + canonical reference, 3 = derived plans, 4 = derived summaries and non-authoritative inputs.",
      "minimum": 0,
      "maximum": 4
    },
    "authority": {
      "type": "string",
      "description": "What this document is allowed to decide. See META-SPEC section 2.",
      "enum": [
        "constitution",
        "concept",
        "implementation",
        "taxonomy",
        "delivery",
        "derived",
        "summary",
        "research",
        "historical"
      ],
      "x-may-originate": {
        "$comment": "Which authorities may appear on a document that declares `decides`. The four omitted values (derived, summary, research, historical) are licensed to originate nothing, so a `decides` list on one of them is a contradiction; scripts/validate_specs.py reads this list and fails the build. This is the COARSE gate only -- it says an authority may originate something, not that a given decision falls within that authority's subject matter. Whether a constitution document is deciding about documents (permitted) or about the product (not) is a judgement no validator makes. `delivery` (D-028) joined in v0.2.10: time-boxed operational policy that expires with its artifact, licensed at tier 3.",
        "values": [
          "constitution",
          "concept",
          "implementation",
          "taxonomy",
          "delivery"
        ]
      }
    },
    "status": {
      "type": "string",
      "enum": ["ACTIVE", "DRAFT", "SUPERSEDED", "HISTORICAL"]
    },
    "doc_set_version": {
      "type": "string",
      "description": "Semantic version of the doc SET this file is aligned to. The set carries one version; individual files do not version independently.",
      "pattern": "^[0-9]+\\.[0-9]+\\.[0-9]+$"
    },
    "last_updated": {
      "type": "string",
      "description": "Month the file was last substantively edited, as YYYY-MM.",
      "pattern": "^[0-9]{4}-(0[1-9]|1[0-2])$"
    },
    "owner": {
      "type": "string",
      "description": "GitHub handle accountable for the document's content.",
      "minLength": 1
    },
    "derives_from": {
      "type": "array",
      "description": "doc_ids this document derives decisions from. A document may never contradict anything it derives from.",
      "items": { "type": "string", "pattern": "^[A-Z][A-Z0-9-]{2,39}$" }
    },
    "supersedes": {
      "type": "array",
      "description": "doc_ids this document replaces as an authority.",
      "items": { "type": "string", "pattern": "^[A-Z][A-Z0-9-]{2,39}$" }
    },
    "decides": {
      "type": "array",
      "description": "Decision IDs (D-nnn) this document is the origin of. Only tier 0-2 documents may claim these, and only when their `authority` appears in authority.x-may-originate. Registered in specs/meta/decision-register.md.",
      "items": { "type": "string", "pattern": "^D-[0-9]{3}$" }
    },
    "enforcement": {
      "$comment": "OPTIONAL IN v0.2.10, BY DESIGN, AND ONLY UNTIL THE SET IS MIGRATED. The enum below binds whenever the key is present, so a wrong value fails today; a MISSING value does not, because you cannot switch on a required field before populating it -- T6 (PLZG-136) assigns all 24 documents, and only then does T3 (PLZG-134) make absence fail. If you are reading this after PLZG-136 landed and absence still passes, that is the bug.",
      "type": "string",
      "description": "How far this document's claims about state are actually proven. Per document, valued at its WEAKEST state claim. See META-SPEC section 2.1 (D-027). `enforced` = a named CI gate re-derives every state claim on every run. `asserted` = a human verified it at a stated date, nothing re-checks. `intended` = aspiration, explicitly not yet true. `n/a` = the document makes no claims about state, which is off the scale rather than its bottom rung. Governs claims about the repository, the board, the running system and the tooling -- NOT decisions, which are governed by `authority`. An `intended` value restricts nothing about originating decisions.",
      "enum": ["enforced", "asserted", "intended", "n/a"]
    },
    "gates": {
      "$comment": "ENCODED AS `job:type` STRINGS, NOT AS {job, type} OBJECTS, AND THE DIFFERENCE IS FORCED. parse_frontmatter() in scripts/validate_specs.py supports `key: scalar` and `key: [a, b]` only -- a deliberately narrow YAML subset, so the contract stays readable without a YAML library. A nested block list errors loudly ('nested frontmatter is not supported'), which is survivable; the inline form `[{job: X, type: live}]` SILENTLY MISPARSES to ['{job: X', 'type: live}'], which is not. The pattern below rejects both, so the dangerous shape fails the build rather than validating as nonsense. This deviates from the wording in sprint-3-charter.md section 4 check 2 ('each typed live or snapshot') in encoding only -- the job name and the type are both still carried and both still checked. Raised on PLZG-133 rather than reconciled silently.",
      "type": "array",
      "description": "CI gates backing this document's state claims, each as `<job name>:<type>` -- for example `Validate Specs:live`. The job name must exist in .github/workflows/ci.yml. Type `live` means the gate re-derives the fact from the system that owns it; `snapshot` means it reads committed data. `enforced` requires at least one `live` entry, because a snapshot gate re-reads without re-checking and so caps at `asserted` (D-027, rule 4). Required non-empty for `enforced` and `asserted` -- enforced in scripts/validate_specs.py, not here, because this schema subset has no conditional support and a `minItems` the validator silently ignored would read as coverage it does not have.",
      "items": {
        "type": "string",
        "pattern": "^[^:]+:(live|snapshot)$"
      }
    },
    "weakest_claim": {
      "$comment": "The falsifiability hook, and the reason this axis is not self-certification. A reviewer checks the quote against the document in ten seconds, and a fabricated audit fails the build. KNOWN LIMIT, STATED SO IT IS NOT REDISCOVERED AS A DEFECT: the validator proves the quote is REAL, never that it is the WEAKEST -- a document may cite a trivially true sentence and still pass. That is the third instance of the same class (authority cannot check subject matter, constitution cannot check whether a decision is product-shaped), and review is still the check. Accepted cost: this duplicates prose into frontmatter, so editing that sentence breaks the build until this is updated -- chosen knowingly, since editing your weakest claim is exactly when the value should be re-examined.",
      "type": "string",
      "description": "For `asserted` and `intended` documents: the single weakest state claim the document makes, quoted so it appears VERBATIM in the document body. Substring match, checked by scripts/validate_specs.py.",
      "minLength": 10
    }
  }
}
