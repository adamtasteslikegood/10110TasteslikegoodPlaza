name: CI Pipeline

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main, dev ]

jobs:
  python-lint:
    name: Lint Python Bridge
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 black websockets
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          
      - name: Lint with Black
        run: |
          black --check .
          
      - name: Lint with flake8
        run: |
          # stop the build if there are Python syntax errors or undefined names
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
          # exit-zero treats all errors as warnings
          flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  godot-build:
    name: Export Godot 4 Prototype
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # Placeholder: Real Godot export will be configured here once the project file (project.godot) is added.
      # You can use 'firebelley/godot-export' or similar actions.
      - name: Godot Web Export (Stub)
        run: |
          echo "Godot project not initialized yet. Skipping actual export."
          # Once initialized, use something like:
          # uses: firebelley/godot-export@v5.2.1
          # with:
          #   godot_executable_download_url: https://downloads.tuxfamily.org/godotengine/4.2.1/Godot_v4.2.1-stable_linux.x86_64.zip
          #   godot_export_templates_download_url: https://downloads.tuxfamily.org/godotengine/4.2.1/Godot_v4.2.1-stable_export_templates.tpz
          #   relative_project_path: ./
          #   archive_output: true
